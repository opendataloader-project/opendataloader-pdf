name: Build Open Dataloader

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: "11"
          distribution: "temurin"

      - name: Clone and build duallab-lr-verapdf-parser
        run: |
          git clone https://github.com/open-dataloader-project/duallab-lr-verapdf-parser.git
          cd duallab-lr-verapdf-parser
          mvn clean install
          cd ..

      - name: Clone and build duallab-layout-recognition-algs
        run: |
          git clone https://github.com/open-dataloader-project/duallab-layout-recognition-algs.git
          cd duallab-layout-recognition-algs
          mvn clean install
          cd ..

      - name: Clone and build duallab-lr-verapdf-validation
        run: |
          git clone https://github.com/open-dataloader-project/duallab-lr-verapdf-validation.git
          cd duallab-lr-verapdf-validation
          mvn clean install
          cd ..

      - name: Build and test duallab-layout-recognition-core
        run: |
          git clone https://github.com/open-dataloader-project/duallab-layout-recognition-core.git
          cd duallab-layout-recognition-core
          mvn clean install
          cd ..

      - name: Get tag version (e.g., v1.2 → 1.2)
        id: version
        run: |
          VERSION=${GITHUB_REF_NAME#v}  # "v1.2" → "1.2"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Rename JAR file with version
        id: jar
        run: |
          ORIG_JAR=duallab-layout-recognition-core/layout-recognition/target/layout-recognition-core-1.2.jar
          VERSION=${{ steps.version.outputs.version }}
          NEW_JAR="layout-recognition-core-${VERSION}.jar"
          cp "$ORIG_JAR" "$NEW_JAR"
          echo "jar_path=$NEW_JAR" >> $GITHUB_OUTPUT

      - name: Create GitHub Release and Upload JAR
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: ${{ steps.jar.outputs.jar_path }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
